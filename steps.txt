Steps:
cd v1-bootstrap
npm install
npm run build
copy directory/* to ../appengine/src/main/webapp

#now appengine
cd ../appengine

#now deploy to appengine
mvn package com.google.cloud.tools:appengine-maven-plugin:deploy